---
description: 从 PRD 到交付的完整 8 步研发流程
argument-hint: <PRD目录路径> [--skip 5,6] [--from 4] [--only 3] [--resume] [--overview]
allowed-tools: ["Read", "Write", "Edit", "Glob", "Grep", "AskUserQuestion", "TodoWrite", "Skill", "Bash"]
---

# dev-flow · 研发流程编排

**你是一个研发流程编排器。** 根据用户的指令，按 8 步工作流推进，每步加载对应 skill 执行。

## 参数解析

$ARGUMENTS 可能包含：

1. **PRD 目录路径**（必须）：PRD 文件所在的目录（PDF/MD/HTML 文件）
2. **流程控制参数**（可选）：
   - `--skip N,M`：跳过指定步骤（如 `--skip 5,6` 跳过编码和自测）
   - `--from N`：从第 N 步开始（如 `--from 4` 从技术方案开始）
   - `--only N`：只执行第 N 步
   - `--resume`：从上次中断的步骤继续（读取输出目录的 `.dev-flow-state.json`）
   - `--overview`：维护工作区 `总览.md` 进度索引（开始登记「在办」、完成挪到「已完成」）。**不传则完全不碰 `总览.md`**。详见「总览维护」

**如果 $ARGUMENTS 为空：**
- 使用当前工作目录作为 PRD 目录
- 从步骤 1 开始；因未传显式流程参数，读完 PRD 后触发第 1.5 步自动路径判断

**自动路径判断触发规则：**
- **无显式流程参数时**：读完 PRD（第 1 步）后触发「第 1.5 步：路径判断」，由插件推荐一条执行路径并请用户确认。
- **传入 `--skip`、`--from`、`--only`、`--resume` 任一时**：跳过自动判断，直接按显式参数执行（显式参数优先）。

## 会话恢复

状态文件路径：`<session_dir>/.dev-flow-state.json`

状态文件格式：
```json
{
  "prd_dir": "D:/path/to/prd_root/项目名/已交付/20260708",
  "session_dir": "D:/path/to/prd_root/Codex/项目名/20260708/ben__20260708114237",
  "path_tier": "standard",
  "path_sequence": [1, 2, 3, 4, 5, 6, 7, 8],
  "skipped_steps": [],
  "current_step": 4,
  "completed_steps": [1, 2, 3],
  "generated_files": {
    "qa_record": "问答记录.md",
    "glossary": "术语表.md",
    "plans": ["任务1_ID001/计划.md", "任务2_ID002/计划.md"]
  },
  "last_updated": "2026-06-30T10:30:00Z"
}
```

**恢复逻辑：**
1. `--resume` 参数触发时，读取当前批次目录（`YYYYMMDD` 或旧格式 `<person>_YYYYMMDD`）下最新的会话目录的 `.dev-flow-state.json`
2. 向用户确认："检测到上次进度到步骤 N（已完成步骤 X, Y, Z），是否继续？"
3. 用户确认后，从 `current_step` 开始执行
4. **按 `path_tier` 恢复路径**：读取状态文件中的 `path_tier`/`path_sequence`/`skipped_steps`，后续步骤按原选定路径推进，不再重新触发第 1.5 步的自动判断
5. 每步完成后立即更新状态文件

**参数组合优先级：**

`--resume` 与其他流程控制参数组合使用时，按下表决定行为（原则：`--from`/`--only` 这类显式起点/范围参数覆盖恢复状态，`--skip` 与恢复叠加）：

| 组合 | 行为 |
|------|------|
| `--resume`（单独） | 从状态文件的 `current_step` 继续，沿用原 `skipped_steps`/`path_tier` 设置 |
| `--resume --from N` | **忽略状态文件**，从第 N 步开始（等同 `--from N`）；给出提示「已忽略上次进度，从第 N 步开始」 |
| `--resume --skip N,M` | 在恢复基础上**追加**跳过步骤 N、M（与状态文件原有 skip 合并） |
| `--resume --only N` | **忽略状态文件**，只执行第 N 步（等同 `--only N`）；给出提示「已忽略上次进度，仅执行第 N 步」 |
| `--resume` 但无状态文件 | 提示「未找到上次进度」，降级为从步骤 1 重新开始 |

## 执行流程

### 第 0 步：环境初始化

1. **检查恢复状态**（最先执行）：
   - 如果传入了 `--resume` 参数，先按已有配置（项目优先，其次全局）读取 `person`，用批次目录（`YYYYMMDD` 或旧格式 `<person>_YYYYMMDD`）定位最近的 Codex 输出会话目录；读不到 `person` 时按子步骤 3 的规则解析后再定位
   - 如果有会话目录包含 `.dev-flow-state.json`：
     - 读取状态文件，向用户确认是否恢复
     - 用户确认恢复 → 跳到 `current_step` 继续执行
     - 用户选择重新开始 → 删除状态文件，创建新的会话目录，从步骤 1 开始

2. **确定 PRD 来源**（三种情况）：

   **情况 A：路径指向文件**
   - 如果 $ARGUMENTS 指向一个 PDF/MD/HTML 文件，该文件就是 PRD
   - PRD 目录 = 该文件所在目录

   **情况 B：路径指向目录**
   - 如果 $ARGUMENTS 指向一个目录，或当前工作目录
   - 用 Glob 扫描目录中的所有 `.pdf`、`.md`、`.html`、`.htm` 文件
   - 如果找到文件 → PRD 就是这些文件

   **情况 C：没有文件（对话输入）**
   - 如果目录为空，或用户明确说 PRD 是对话内容
   - 从对话上下文获取 PRD 内容
   - 用 AskUserQuestion 让用户确认/补充 PRD 内容

3. **解析个人标识（person）**：

   后续批次目录、会话目录的前缀和文档署名都用到 `person`，须先确定它。

   - **解析顺序**：先读项目（PRD 根目录）`.codex/dev-flow.config.md`，再读全局 `~/.codex/dev-flow.config.md`，取 frontmatter 中的 `person` 字段（项目配置优先）。
   - **判定「未配置」**：两处都没有该文件，或文件存在但 `person` 字段缺失/为空。
   - **未配置时（优先询问并记录）**：
     - 用 AskUserQuestion 询问用户的个人标识（如 `ben`），说明它将用于批次/会话目录前缀和文档署名。
     - 拿到后**写入全局** `~/.codex/dev-flow.config.md`：文件不存在则新建，**仅写入 frontmatter**（参照 `config/default-config.md` 中 `---` 之间的字段，不要把 `## 说明` 等散文写进去）并填入 `person`；已存在则补充/修改 `person` 字段。
   - **已配置则直接使用**，不再询问。
   - 若 PRD 路径中的批次目录名（如 `ben_20260630`）与配置的 `person` 不一致，以配置的 `person` 为准，并提示用户。
   - 解析出的 `person` 即为后续所有输出文档（问答记录/计划/总结）的「负责人」署名。

4. **确定批次目录**：

   批次目录用于归集同一批次的多次会话，命名格式为 `YYYYMMDD` 或 `YYYYMMDD_N`（同一天第 N 批）。旧项目可能使用 `<person>_YYYYMMDD` 格式，新项目统一不带 person 前缀。

   **批次目录由用户手动创建**，代表一次独立的需求分配。dev-flow 只负责从路径中识别批次目录名，并在 Codex/ 下镜像创建同名目录。

   **检测逻辑：**

   1. **从 PRD 路径提取**（最常用）：
      - PRD 路径形如 `Prd/<项目>/<状态>/YYYYMMDD/`，取日期目录名作为批次目录名
      - 例：`Prd/sui-yin-ji/已交付/20260708/` → 批次目录名 = `20260708`
      - 例：`Prd/亲亲创客合伙人二期/已交付/ben_20260630/` → 批次目录名 = `ben_20260630`（旧格式兼容）
      - Codex 输出镜像创建同名批次目录

   2. **路径中不含批次目录**：
      - 提示用户：请先在 Prd/<项目>/<状态>/ 下创建日期目录（如 `20260717/`）并写好 `需求.md`，再从该目录运行 dev-flow
      - 或用 AskUserQuestion 让用户指定批次目录名

   **同一天多批次示例：**
   ```
   Prd/项目名/已交付/
   ├── 20260708/         ← 第一批
   ├── 20260708_02/      ← 第二批：同一天又分到新需求
   └── 20260708_03/      ← 第三批
   ```

5. **确定输出目录**：

   **情况 A：有项目结构**（路径形如 `root/Prd/项目名/<状态>/YYYYMMDD/`）
   - 输出到 `root/Codex/项目名/YYYYMMDD/`
   - 例：`ben-workspace/Prd/sui-yin-ji/已交付/20260708/` → `ben-workspace/Codex/sui-yin-ji/20260708/`

   **情况 B：没有项目结构**（普通目录或文件）
   - 输出到 `<PRD目录>/../Codex/YYYYMMDD/`
   - 例：`D:/work/prd/` → `D:/work/Codex/20260708/`

   **情况 C：对话输入**（无文件）
   - 输出到 `./output/Codex/YYYYMMDD/`（当前工作目录）

   **配置文件优先**：
   - 检查是否有 `.codex/dev-flow.config.md` 配置文件
   - 如果配置了 `codex_output` 绝对路径，直接使用
   - 否则按上述规则推断
   - 用 AskUserQuestion 向用户确认输出目录

6. **创建会话目录**：
   - 在批次目录下创建 `<person>__<YYYYMMDDHHmmss>` 格式的会话目录
   - 例：`ben_20260630/ben__20260630153000`
   - 所有流程产出的文档都写在这个会话目录里
   - 每次运行新建一个会话目录，不会覆盖之前的输出

7. 创建输出目录（如果不存在）

8. 用 TodoWrite 创建 8 步任务清单，标记已完成和待执行的步骤

9. **初始化状态文件**：写入 `<会话目录>/.dev-flow-state.json`，`current_step: 1`

### 第 1 步：读 PRD

**加载 skill：** 使用 Skill 工具加载 `dev-flow:read-prd`

按 skill 指导：
- 读取所有 PRD 文件（MD/HTML 用 Read 直接读，PDF 用 Read 的 pages 参数）
- **PRD 内嵌截图/原型是视觉规格来源**：与散文需求同等对待，按 read-prd 的视觉规格五要素逐元素提取（列归属/合并、按钮精确文案、所在页签、筛选位置、字段顺序）；图与正文冲突或图更细时以图为准
- 提取每个需求的核心要点
- 识别任务拆分和 ID
- 输出需求理解摘要，向用户确认
- 若传入 `--overview`：需求确认后，把需求登记到工作区 `总览.md` 的「🔥 在办」（🔧进行中），见「总览维护」

完成后更新 `.dev-flow-state.json`。

### 第 1.5 步：路径判断

> 读完 PRD 后自动评估任务复杂度，推荐一条执行路径，减少简单任务的流程 overhead。判断时机在读完 PRD 之后（此时才能准确判断复杂度），不在调用前。

**触发条件：**
- **仅在用户未传显式流程参数时执行**：无 `--skip`、`--from`、`--only`、`--resume`。
- **传入上述任一参数时**：跳过本步，直接按显式参数执行（显式参数优先）；`--resume` 则按状态文件的 `path_tier` 恢复。

**三条路径定义：**

| 路径 | 执行序列 | 跳过步骤 | 适用 |
|------|---------|---------|------|
| 快速路径（quick） | 1 → 4 → 5 → 6 → 7 → 8 | 2（grill）、3（问答记录） | 需求清晰、改动单一、无歧义 |
| 标准路径（standard） | 1 → 2 → 3 → 4 → 5 → 6 → 7 → 8 | 无 | 有不确定点、跨模块或接口变更 |
| 文档路径（doc） | 1 → 2 → 3 → 4 → 8 | 5（编码）、6（自测）、7（code review） | 纯文档/配置/方法论改动，无可执行代码 |

三条路径都保留步骤 4（tech-plan）和步骤 8（总结），这两步始终执行。文档路径跳过的是为「可执行代码」设计的 implement/self-test/code-review 三个 skill，文档本身的实际修改仍在方案确认后落地。

**判定规则（确定性，分两级，按顺序判断，不依赖自由发挥）：**

1. **一级 —— 是否文档路径**：
   - 若本次改动**不涉及可执行代码**（纯文档、配置、方法论；无测试可跑）→ **文档路径（doc）**，判断结束。
   - 否则进入二级判断。

2. **二级 —— 快速 vs 标准**：
   - 定义「标准信号」集合：
     - a) 需求含模糊 / 待确认 / 暂定措辞；
     - b) 改动跨模块，或涉及接口 / Schema 变更；
     - c) 有多个任务，或任务间存在依赖。
   - **命中任一标准信号 → 标准路径（standard）**。
   - **三项全部为快速信号（无歧义、单文件或单模块、单个独立任务）→ 快速路径（quick）**。

**用户确认交互：**
使用 AskUserQuestion，展示：路径名 + 判断理由 + 将执行的步骤序列。参考格式：

```
读完 PRD，我的判断：

路径：快速路径（跳过 grill 和问答记录）
理由：需求描述明确，改动范围仅限 read-prd/SKILL.md，无歧义点需要对齐。
执行步骤将是：1 → 4 → 5 → 6 → 7 → 8

选项：
A. 确认，按推荐路径执行
B. 改为标准路径（加入 grill 步骤）
C. 改为文档路径（跳过编码相关步骤）
```

- 选项至少包含「确认按推荐路径执行」与「改为标准路径」两项。
- 用户确认后立即进入选定路径的下一步，不再重复询问。

**确认后处理：**
- 将选定路径写入 `.dev-flow-state.json`：`path_tier`（quick/standard/doc）、`path_sequence`（如 `[1,2,3,4,8]`）、`skipped_steps`（如 `[5,6,7]`）。
- 用 TodoWrite 更新任务清单：被跳过的步骤标记为 completed，content 备注「（跳过·<路径名>）」，复用「重要规则 4」的跳步机制。

完成后更新 `.dev-flow-state.json`。

### 第 2 步：grill me

**加载 skill：** 使用 Skill 工具加载 `dev-flow:grill`

按 grill skill 指导：
- 用**决策树遍历**方法，沿设计决策向下走，逐个解决分支间依赖
- 每个问题先给推荐答案，再让用户确认
- 一次只问一个问题
- 先查代码库，再问用户
- **同步构建术语表**：发现术语歧义时立即追问并记录，grill 完成后输出 `<session_dir>/术语表.md`

完成后更新 `.dev-flow-state.json`。

### 第 3 步：问答记录

**加载 skill：** 使用 Skill 工具加载 `dev-flow:qa-record`

按 skill 指导：
- 将 grill 阶段的所有提问和回答整理成结构化的 `问答记录.md`
- 写入输出目录：`<session_dir>/问答记录.md`

完成后更新 `.dev-flow-state.json`。

### 第 4 步：技术方案

**加载 skill：** 使用 Skill 工具加载 `dev-flow:tech-plan`

按 skill 指导：
- 为每个任务生成 `计划.md`，采用**行为化描述**风格
- 包含：目标、验收标准、任务拆解、边界/依赖（含作用域外）
- **视觉规格按图写死进验收标准**：PRD 图或正文已给出的位置/文案直接写成验收标准，不列入待确认；只有图和正文都没写的才算真待确认（详见 tech-plan「图已定 vs 真待确认」）
- 写入：`<session_dir>/<任务名_ID>/计划.md`
- 写完后用 AskUserQuestion 向用户确认方案

完成后更新 `.dev-flow-state.json`。

### 第 5 步：编码

**加载 skill：** 使用 Skill 工具加载 `dev-flow:implement`

按 implement skill 指导：
- 将技术方案拆分为**垂直切片**（每个切片走通全链路：数据→接口→展示→测试）
- 有测试框架时跑 **TDD 循环**：RED → GREEN → REFACTOR
- 测试运行节奏：每次提交前跑类型检查，每个切片完成后跑单文件测试，全部完成后跑完整套件
- 每完成一个切片，更新 `计划.md` 中对应子任务状态
- 遵循项目现有的代码风格和规范（参考 dev-flow 配置文件中的个人偏好）
- **归集非代码交付物**：编码若产生 SQL 脚本、接口文档、上线注意事项等，按需写入会话目录（建议 `<session_dir>/交付物/`）；没有则不硬造

完成后更新 `.dev-flow-state.json`。

### 第 6 步：自测

**加载 skill：** 使用 Skill 工具加载 `dev-flow:self-test`

按 skill 指导：
- 先检查**测试质量**（测试是否通过公共接口验证行为，是否能扛住重构）
- 逐条对照计划.md 的验收标准检查是否满足
- 检查边界条件、数据一致性、依赖、代码质量、兼容性
- 输出自测结果

完成后更新 `.dev-flow-state.json`。

### 第 7 步：code review

**加载 skill：** 使用 Skill 工具加载 `dev-flow:code-review`

按 skill 指导：
- **审查前验证**（先跑代码）：
  - 自动检测项目测试配置（package.json 的 test script 等）
  - 有测试则先跑，将结果纳入审查报告
  - 无测试则跳过，标注"仅做静态审查"
- 逐文件审查：正确性、边界安全、性能、规范、可维护性
- 输出审查报告
- 如有 🔴 必须修复项，回到第 5 步修复

完成后更新 `.dev-flow-state.json`。

### 第 8 步：总结

**加载 skill：** 使用 Skill 工具加载 `dev-flow:summary`

按 skill 指导：
- 生成开发总结 `总结.md`
- 包含：交付文件清单（含会话目录内的 SQL/接口文档/注意事项等交付物）、上线依赖、设计要点、待联调项
- 写入：`<session_dir>/总结.md`
- 更新每个任务的 `计划.md` 状态为「已完成（详见 ../总结.md）」
- **维护工作区 `总览.md`**（仅当传入 `--overview`，不存在则新建）：把本次需求从「在办」移到「已完成」，见「总览维护」与 summary skill

完成后删除 `.dev-flow-state.json`（流程已结束）。

## 重要规则

1. **每步完成后立即保存输出文件**，不要等到最后
2. **每步完成后更新 `.dev-flow-state.json`**，记录当前进度
3. **每步完成后用 TodoWrite 更新进度**
4. **跳步时**：直接标记跳过的步骤为已完成，不执行（第 1.5 步路径判断产生的跳过也复用此机制）
5. **用户中断**：状态文件已保存，下次用 `--resume` 可继续
6. **语言**：所有输出文档使用中文
7. **确认机制**：关键节点（第 1.5 步路径判断、步骤 2、4、7）完成后必须向用户确认再继续
8. **自动路径判断**：无显式流程参数（`--skip`/`--from`/`--only`/`--resume`）时，第 1 步后执行第 1.5 步的自动路径判断；有则跳过

## 输出目录结构

### 场景 A：有项目结构

```
ben-workspace/
├── Prd/
│   └── 项目名/                         ← PRD 项目
│       ├── 初始化/                       ← 状态目录
│       ├── 进行中/
│       ├── 已交付/
│       │   ├── 20260708/                ← 日期目录（含 需求.md / 图片）
│       │   └── 20260708_02/             ← 同一天第二批
│       └── 待排期/
│
└── Codex/
    └── 项目名/                          ← 镜像 PRD 的项目层级
        ├── 20260708/                    ← 批次目录（镜像 PRD 日期目录名）
        │   └── ben__20260708114237/     ← 会话目录
        │       ├── .dev-flow-state.json
        │       ├── 术语表.md
        │       ├── 问答记录.md
        │       ├── 任务名_ID001/计划.md
        │       └── 总结.md
        └── 20260708_02/
            └── ben__20260708151602/     ← 同一天的新会话
                └── ...
```

### 场景 B：没有项目结构（普通目录）

```
D:/work/my-project/
├── prd/                                ← PRD 目录（或单个文件）
│   └── 需求.md
│
└── Codex/
    └── ben_20260630/                   ← 批次目录（按当天日期）
        └── ben__20260630153000/        ← 会话目录
            └── ...
```

### 场景 C：无文件（对话输入）

```
当前工作目录/
└── output/
    └── Codex/
        └── ben_20260630/               ← 批次目录
            └── ben__20260630153000/    ← 会话目录
                └── ...
```

## 输出文件命名规范

- 批次目录：`YYYYMMDD`（如 `20260708`，与 PRD 日期目录名一致；旧项目可能带 `<person>_` 前缀）
- 会话目录：`<person>__<YYYYMMDDHHmmss>`（时间戳精确到秒，在批次目录下）
- 问答记录：`问答记录.md`（固定文件名）
- 术语表：`术语表.md`（固定文件名）
- 计划：`<任务简称_ID<任务编号>/计划.md`
  - 从 PRD 文件名或内容中提取任务名和 ID
  - 如：`产品配置_ID1019077/计划.md`
- 总结：`总结.md`（固定文件名）
- 状态文件：`.dev-flow-state.json`（流程结束后删除）

## 总览维护（可选，需 `--overview`）

**默认不碰 `总览.md`。仅当传入 `--overview` 才执行本节。** 两个触点：开始（第 1 步需求确认后）登记到「🔥 在办」（🔧进行中）；收尾（第 8 步）把需求移到对应项目的「✅ 已完成」。

1. **定位**：从 PRD 路径找到 `Prd/` 所在层级，其父目录即工作区根，`总览.md` 就在此处。存在则更新；不存在则用下方模板新建。**路径中不含 `Prd/` 段（一句话需求 / 对话输入）→ 跳过（无处定位工作区根）。**
2. **项目分组**：取 `Prd/` 下第一层目录名作为项目名（如 `Prd/sui-yin-ji/已交付/20260708/` → `sui-yin-ji`；`Prd/codex-dev-workflow/01dev-flow/已交付/20260714/` → `codex-dev-workflow`，子项目归入父项目分组）。在「## ✅ 已完成」下找同名 `### <项目名>` 小节；没有则新建该小节和表头。
3. **写行**：
   - 在办行（列：需求 ｜ 状态 ｜ PRD）：🔧进行中；需求名取自 PRD 标题或任务名；PRD 为相对 `总览.md` 的链接。
   - 已完成行（列：需求 ｜ 状态 ｜ PRD ｜ 产出）：✅；产出填本次会话目录的相对链接。
   - 搬移：收尾时删掉该需求在「在办」的行，加入对应项目「已完成」小节。
4. **更新顶部** `更新：YYYY-MM-DD` 为当天日期。

新建模板（仅当 `总览.md` 不存在，先建骨架再写入本次需求）：

```markdown
# 总览

> 一行一需求，可点进 PRD 和产出。更新：YYYY-MM-DD
> 状态：🔧进行中 ｜ ⬜未开始 ｜ ✅已交付

## 🔥 在办

| 需求 | 状态 | PRD |
|------|------|-----|
| （暂无） | — | — |

## ✅ 已完成

---

新需求 → 加到「在办」；完成 → 挪进「已完成」对应项目，补上产出链接。
```

## 错误处理

- PRD 目录不存在 → 提示用户检查路径
- PRD 目录为空（无 PDF/MD/HTML） → 提示用户确认目录
- 输出目录创建失败 → 提示权限问题
- 读取 PDF 失败 → 提示用户转换为 MD 或手动提供需求内容
- 遇到 Word（.doc/.docx）等不支持的格式 → 提示用户先另存为 PDF/MD 再运行
- 状态文件损坏 → 提示用户删除状态文件重新开始
